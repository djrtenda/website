rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hanya perbolehkan akses jika user sudah login (Admin)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}